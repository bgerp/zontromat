<?php 
// Извикава се, когато на RFID четеца е пррочетена карта
// Формата php rfid.php -n [номер на картата] -r [номер на четеца]

require __DIR__ . '/lib/boot.php';

// Очакваме да сме извикани по CLI
expect(ZTM_IS_CLI);

// Вземаме опцията -n и -r
$opt = getopt('n:r:');
debug($opt);

// Проверяваме дали картата е в таблицата с разрешените карти
// Ako Да - подаваме цифров импулс за отваряне на вратата и 
// подаваме на четеца да светне зелено. Ако Не - нищо не правим


exit(0);
